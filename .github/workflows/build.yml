name: Build & Release

on:
  push:
    branches: ["**"]
    tags: ["v*"]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_VERSION: "8.0.x"
  DOTNET_NOLOGO: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true

jobs:
  # ── Build & test ─────────────────────────────────────────────────────────────
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore EDS.sln

      - name: Build
        run: dotnet build EDS.sln --no-restore --configuration Release

      - name: Test
        run: dotnet test EDS.sln --no-build --configuration Release --logger "console;verbosity=normal" --filter "Category!=E2E"

  # ── Publish cross-platform binaries (version tags only) ──────────────────────
  publish:
    name: Publish (${{ matrix.rid }})
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - rid: osx-arm64
            archive: eds-osx-arm64.tar.gz
          - rid: linux-x64
            archive: eds-linux-x64.tar.gz
          - rid: win-x64
            archive: eds-win-x64.zip

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Publish ${{ matrix.rid }}
        run: |
          VERSION="${{ github.ref_name }}"
          # Strip leading 'v' so dotnet gets a plain semver (e.g. 0.8.0-beta)
          VERSION="${VERSION#v}"
          dotnet publish src/EDS.Cli \
            -r ${{ matrix.rid }} \
            -o publish/${{ matrix.rid }} \
            -p:Version="${VERSION}"

      - name: Package binary
        run: |
          OUT="publish/${{ matrix.rid }}"
          if [ "${{ matrix.rid }}" = "win-x64" ]; then
            # zip preserves nothing on Windows — fine, .exe has no execute concept
            cd "$OUT" && zip -j "../../${{ matrix.archive }}" eds.exe
          else
            # tar.gz preserves the execute bit set by dotnet publish
            cd "$OUT" && tar czf "../../${{ matrix.archive }}" eds
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive }}
          path: ${{ matrix.archive }}
          if-no-files-found: error

  # ── GitHub Release (version tags only) ───────────────────────────────────────
  release:
    name: Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: publish
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history so git log can reach the previous tag

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Build release notes
        id: notes
        run: |
          PREV=$(git tag --sort=-version:refname | grep -E '^v[0-9]' | sed -n '2p')
          if [ -n "$PREV" ]; then
            RANGE="${PREV}..HEAD"
            HEADER="Changes since ${PREV}"
          else
            RANGE="HEAD"
            HEADER="Initial release"
          fi
          NOTES=$(git log "$RANGE" --pretty=format:"- %s (%h)" --no-merges)
          {
            echo "body<<EOF"
            echo "## ${HEADER}"
            echo ""
            echo "${NOTES}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body: ${{ steps.notes.outputs.body }}
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') }}
          files: artifacts/*
